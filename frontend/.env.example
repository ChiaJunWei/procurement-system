# Base URL of the Spring Boot API gateway. Leave the mock flag on to run standalone.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081
# When "true", the typed apiClient resolves registered mock handlers instead of calling the backend.
NEXT_PUBLIC_USE_MOCK=true
